import { SentimentType } from "@/lib/db/schema";

type Props = {
  sentiment: SentimentType[]
};

const SidebarComponent = ({sentiment}: Props) => {
  return (
    <div  className=""></div>
  )
}

export default SidebarComponent;